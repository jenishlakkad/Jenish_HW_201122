package org.example;

import org.openqa.selenium.WebDriver;

public class BaseFile
{  //Base File is first base class of the project
    protected static WebDriver driver;
    //we are using webdriver service by selenium
}
